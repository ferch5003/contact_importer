<tr>
  <td><%= contact.name %></td>
  <td><%= contact.email %></td>
  <td><%= contact.telephone %></td>
  <td><%= contact.address %></td>
  <td><%= contact.date_of_birth %></td>
  <td><%= show_credit_card_hint(last_digits: contact.card_last_digits) %></td>
  <td><%= contact.franchise %></td>
  <td><%= contact.created_at %></td>
</tr>